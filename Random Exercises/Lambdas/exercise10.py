# Exercise 10
# Explain in words what this does:
# sorted(data, key=lambda x: x[0])

# For each element in data, sorted() calls the lambda, which returns x[0]. Those returned values are used to determine the order.